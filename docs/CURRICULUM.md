# Curriculum — Data Minds SQL Academy

**Status:** Phase 0 structure. Content authored from Phase 3 using [CONTENT_GUIDELINES.md](CONTENT_GUIDELINES.md) and the `create-section` / `create-exercise` skills.

## 1. Learning path structure

One course ("Ruta SQL para Analistas de Datos") organized in **6 levels** and **40 sections**. Each section = objectives + concise theory (1–3 lessons) + worked examples + common mistakes + 4–7 practical exercises + 8–15 theory questions (of which 5, 6 or 10 are served per quiz attempt — see §3) + 1 section challenge + completion rule.

| Level                        | Sections                                                                                                                                                                                           | Certificate                                           |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| **N1 · Fundamentos**         | 1 Introducción a bases de datos y SQL · 2 Tablas, filas, columnas y tipos de datos · 3 SELECT · 4 Alias y expresiones · 5 DISTINCT · 6 WHERE · 7 Operadores de comparación y lógicos · 8 NULL      | **Certificado: Fundamentos de SQL**                   |
| **N2 · Transformar datos**   | 9 Funciones de texto · 10 Funciones numéricas · 11 Fechas y horas · 12 CASE · 13 Ordenar y limitar resultados                                                                                      | —                                                     |
| **N3 · Agregar y combinar**  | 14 Funciones de agregación · 15 GROUP BY · 16 HAVING · 17 INNER JOIN · 18 LEFT, RIGHT y FULL JOIN · 19 Self joins · 20 Joins de múltiples tablas                                                   | **Certificado: SQL para Análisis de Negocio**         |
| **N4 · Consultas avanzadas** | 21 Subconsultas · 22 CTE · 23 Operaciones de conjuntos · 24 Agregación condicional · 25 Funciones de ventana · 26 Funciones de ranking · 27 Totales acumulados y promedios móviles · 28 LAG y LEAD | **Certificado: SQL Analítico Avanzado**               |
| **N5 · Analítica aplicada**  | 29 Cohortes y retención · 30 Funnels · 31 Deduplicación · 32 Investigaciones de calidad de datos · 33 Depuración de consultas · 34 Fundamentos de optimización · 35 Índices y planes de ejecución  | —                                                     |
| **N6 · Profesional**         | 36 SQL analítico avanzado · 37 Casos de negocio · 38 Desafíos de entrevista técnica · 39 SQL con IA · 40 Proyectos finales (capstone)                                                              | **Certificado: Analista SQL Profesional** (full path) |

Prerequisites are linear inside a level; levels unlock when the previous level's section challenges are passed (mastery learning). Learners can _preview_ any lesson's theory but not submit gated exercises.

Free tier: the first 5 practical exercises (`FREE_EXERCISE_LIMIT`, sections 2–3) plus all theory of N1 are free; everything else requires an entitlement (D-01).

## 2. Difficulty scale

| Label      | Meaning                                                                             | Typical time |
| ---------- | ----------------------------------------------------------------------------------- | ------------ |
| Muy fácil  | One concept, one table, direct instruction                                          | 2–4 min      |
| Fácil      | One or two concepts, one table, light filtering                                     | 4–7 min      |
| Intermedio | Two or more concepts combined; joins/aggregation; edge cases (NULL, dates)          | 7–12 min     |
| Avanzado   | Multi-step reasoning, CTEs/windows, ambiguity to resolve from the business question | 12–20 min    |
| Experto    | Realistic ambiguous business request, data-quality traps, performance awareness     | 20–40 min    |

Every difficult exercise must test a named skill; complexity for its own sake is rejected during review.

## 3. Initial content volume (MVP release)

Depth over volume. Launch with **8 fully authored sections** spanning free and paid tiers and all three MVP datasets:

| Section                                             | Exercises                                 | Questions          | Dataset(s)           |
| --------------------------------------------------- | ----------------------------------------- | ------------------ | -------------------- |
| 1 Introducción                                      | 0 (theory + quiz)                         | 10                 | —                    |
| 2 Tablas y tipos                                    | 2 (very easy)                             | 10                 | tiendaviva           |
| 3 SELECT                                            | 5                                         | 10                 | tiendaviva           |
| 6 WHERE + 7 Operadores (merged lesson flow)         | 6                                         | 14                 | tiendaviva, bolsillo |
| 8 NULL                                              | 4                                         | 10                 | bolsillo             |
| 14–15 Agregación y GROUP BY                         | 6                                         | 14                 | pidelo               |
| 17 INNER JOIN                                       | 5                                         | 12                 | tiendaviva           |
| 18 LEFT/RIGHT/FULL JOIN                             | 5                                         | 12                 | pidelo, bolsillo     |
| 25 Funciones de ventana (showcase of advanced tier) | 5                                         | 12                 | bolsillo             |
| **Total**                                           | **38 exercises** (+ 8 section challenges) | **~104 questions** | 3 datasets           |

The table above is the MVP scope note kept for history. **The course is now fully authored.** Counts reported by `npm run content:validate` on 2026-09-23, after section 35 (índices y planes de ejecución) closed the last gap: `courses 1, sections 39 (39 published), lessons 347, questions 414, exercises 209, datasets 4`. `npm run content:verify` on the same state: `OK (209 exercise(s) verified, expected results written)`. Later the same day, the D-37 question banks were deepened (see the quiz-length table above) and the counts became `courses 1, sections 39 (39 published), lessons 347, questions 432, exercises 209, datasets 4`. Every section has objectives, a summary, theory lessons, a question bank and verified exercises, so no section shows "Próximamente" and none is excluded from certificate requirements. Further sections are post-MVP additions, not gaps.

### Quiz length per section (D-37)

The bank is what is authored (8–14 questions); the **attempt** is a sample of it, and how big that
sample is depends on the section. The number is authored in `quizQuestionsBySection`
(`src/content/sections.ts`) and validated by `npm run content:validate` against the bank.

| Length                             | When                                                                                                       | Sections                                                                       |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **5** (check)                      | ≥ 5 authored exercises: the graded queries are the evidence, the quiz probes what a result set cannot show | 28 sections                                                                    |
| **6** (standard, also the default) | ≤ 4 exercises, so the quiz carries more of the judgement                                                   | 1, 2, 4, 5, 6, 7, 14                                                           |
| **10** (gate)                      | closes a level and feeds a certificate                                                                     | 8 `null`, 20 `joins-multiples-tablas`, 28 `lag-y-lead`, 40 `proyectos-finales` |

Two constraints bind the choice and are enforced in code, not by convention:

- Only `limits.quiz.lengthsAllowed` (5, 6, 10, 11, 12) may be declared. At 7, 8 or 9 the 80 % pass
  threshold silently becomes 86–89 %, and at 4 one mistake fails the attempt.
- The published bank must be at least `length + limits.quiz.minUnseenOnRetry` (3), so a retry is a
  new measurement rather than a recall of the feedback the failed attempt showed.

Both tiers are live since 2026-09-23. The four gate sections were taken from 10 to 14 published
questions each (+4, +4, +2, +2), which leaves four questions outside any single 10-question
attempt, and `distinct`, `alias-y-expresiones` and `operadores-comparacion-logicos` went from 8 to
10, which supports the 6 their exercise count calls for. The new gate items are deliberately about
judgement rather than syntax: `IS DISTINCT FROM` and the three-valued truth table in `null`; an
`INNER JOIN` hanging off a `LEFT JOIN`, pre-aggregating two child tables and the average that
silently becomes weighted in `joins-multiples-tablas`; ties in the window `ORDER BY` and the frame
clause that `lag`/`lead` ignore in `lag-y-lead`; a question the data cannot answer and a report that
cannot be reproduced in `proyectos-finales`.

### Published sections (2026-09-22)

Section 24 (agregación condicional) was authored on 2026-09-22: 3 theory lessons, 12 theory questions and 6 verified exercises on `tiendaviva` (intermediate → expert). Counts reported by `npm run content:validate` at that moment, with several sections landing in parallel: 25 of 39 sections published, 193 lessons, 246 theory questions, 111 exercises over 4 datasets. Section 26 (funciones de ranking) was authored on 2026-09-22: 3 theory lessons, 12 theory questions and 6 verified exercises on `ritmo` (intermediate → expert), the first content on that dataset; `ritmo` was also added to the dataset registry in `src/content/index.ts`. Counts at that moment: 27 of 39 sections published, 215 lessons, 270 theory questions, 125 exercises over 4 datasets. Re-scoped on 2026-09-22 after D-20 (UTC session): the expert exercise of section 26 moved from the Mexican market to the Peruvian one (`desafio-top-3-artistas-por-mes-en-peru`, 27 rows) because under a UTC session the Mexican top 3 has no tie at all and `dense_rank`, `rank` and `row_number` returned the identical 24 rows, which graded a `ROW_NUMBER` answer as correct in the exercise whose whole point is tie discrimination. Peru is the smallest `ritmo` market (397 listeners) and Jan-Aug 2025 contains three natural ties inside the top three levels (2025-02 at the third level, 2025-03 and 2025-06 at the second), so the three functions now return 27, 25 and 24 rows respectively. Content change only; the dataset was not touched. Section 27 (totales acumulados y promedios móviles) was authored on 2026-09-22: 3 theory lessons (running totals, the frame clause, moving averages), 12 theory questions covering all eight question types and 7 verified exercises on `ritmo` (intermediate → advanced), the second section on that dataset. Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 27 of 39 sections published, 215 lessons, 270 theory questions, 125 exercises over 4 datasets. Known sandbox gap found while authoring: the parser gate rejects `RANGE BETWEEN INTERVAL '...' PRECEDING` and named `WINDOW` clauses, so the gap-filling exercise uses a `generate_series` calendar instead; see the note for `sql-sandbox-engineer`. Section 23 (operaciones de conjuntos) was authored on 2026-09-22: 3 theory lessons (UNION/UNION ALL, INTERSECT/EXCEPT, precedence and alternatives), 12 theory questions covering all eight question types and 7 verified exercises on `tiendaviva` and `bolsillo` (easy → expert). Counts reported by `npm run content:validate` at that moment: 27 of 39 sections published, 215 lessons, 270 theory questions, 125 exercises over 4 datasets. Authoring it required a sandbox change: `pgsql-ast-parser` has no grammar for `INTERSECT`/`EXCEPT`, so the parser gate rejected every solution of the section; `src/lib/sandbox/gate.ts` now rewrites those keywords to a same-length `UNION` variant **for analysis only** (see D-18 in `docs/DECISIONS.md`). Section 28 (LAG y LEAD) was authored on 2026-09-22: 3 theory lessons (LAG/LEAD basics, period-over-period variation, event gaps and FIRST_VALUE/LAST_VALUE), 12 theory questions covering all eight question types and 7 verified exercises on `ritmo` (intermediate → expert), the third section on that dataset. Counts reported by `npm run content:validate` right after that change, with other sections landing in parallel: 28 of 39 sections published, 226 lessons, 282 theory questions, 132 exercises over 4 datasets. Every solution pins the calendar with `AT TIME ZONE 'UTC'` so results do not depend on the runner's TimeZone, and no solution relies on named `WINDOW` clauses or interval frame bounds, which the parser gate still rejects. Section 29 (cohortes y retencion) was authored on 2026-09-22: 3 theory lessons (what defines a cohort, the retention matrix, N-day retention and churn), 12 theory questions covering all eight question types and 7 verified exercises on `ritmo` (advanced -> expert), the fourth section on that dataset. Counts reported by `npm run content:validate` after that change: 29 of 39 sections published, 237 lessons, 294 theory questions, 139 exercises over 4 datasets. Every solution pins the calendar with `AT TIME ZONE 'UTC'`, every rate keeps the full cohort in the denominator (`LEFT JOIN`/`EXISTS`, never `INNER JOIN`), and every metric restricts the cohorts to those whose observation window closed inside the snapshot; no solution relies on named `WINDOW` clauses or interval frame bounds. Section 30 (funnels) was authored on 2026-09-22: 3 theory lessons (defining the steps of a funnel, strict-ordered versus any-order counting, drop-off and time-to-convert), 12 theory questions covering all eight question types and 7 verified exercises on `pidelo`, `tiendaviva` and `ritmo` (intermediate -> expert). Counts reported by `npm run content:validate` after that change: 30 of 39 sections published, 248 lessons, 306 theory questions, 146 exercises over 4 datasets. The section makes the two honest funnel definitions explicit and measures both on the same milestone table: on `ritmo`, activation reaches 1225 users counted in any order and only 135 counted in strict order, which is the pedagogical core of the section. Every solution pins the calendar with `AT TIME ZONE 'UTC'` (notably when comparing `subscriptions.started_on`, a `date`, with a `timestamptz`), every funnel base is built with `LEFT JOIN`/`EXISTS` so the first step stays the full universe, and no solution relies on named `WINDOW` clauses or interval frame bounds, which the parser gate still rejects. Section 31 (deduplicación) was authored on 2026-09-22: 3 theory lessons (what counts as a duplicate, business-key duplicates, choosing the surviving row), 12 theory questions covering all eight question types and 7 verified exercises on `ritmo` and `tiendaviva` (very_easy -> expert). Counts reported by `npm run content:validate` after that change: 31 of 39 sections published, 259 lessons, 318 theory questions, 153 exercises over 4 datasets. The section insists that the identity key is a business decision before it is SQL: `ritmo` has 340 planted exact-duplicate pairs in `plays` under `(user_id, track_id, played_at)`, and `tiendaviva` has 37 groups of `customers` sharing a normalized email (`lower(btrim(email))`, 39 excess rows), which covers the exact-duplicate and business-key cases on real data. `DISTINCT ON` **passes** the parser gate (checked explicitly against `gateSql`), so the DISTINCT ON exercise uses it as the reference solution with a `ROW_NUMBER` alternative; no solution relies on named `WINDOW` clauses or interval frame bounds. No solution depends on the runner's TimeZone: the section does no date truncation at all. Section 32 (calidad de datos) was authored on 2026-09-22: 3 theory lessons (profiling an unfamiliar table; NULL, blanks and inconsistent categories; validity rules, referential gaps and the check report), 12 theory questions covering all eight question types and 7 verified exercises on `ritmo` and `pidelo` (intermediate -> expert). Counts reported by `npm run content:validate` after that change: 32 of 39 sections published, 270 lessons, 330 theory questions, 160 exercises over 4 datasets. The section is built on documented, verified dataset quirks rather than invented ones: `ritmo.plays` has 340 planted exact-duplicate keys, 1745 NULL `device`, 90 NULL and 120 negative `seconds_played` and 210 rows above the track duration (up to 42x), and `pidelo` has 178 orders with no `order_items` row, 171 of them with `total` > 0. Every solution pins the calendar with `AT TIME ZONE 'UTC'` and none relies on named `WINDOW` clauses or interval frame bounds. Open dataset question raised while authoring: `bolsillo.accounts.balance` is negative in 966 of 4462 accounts (min -15 682 828), which the generator produces by summing completed movements without any funds check; there is no overdraft or credit-limit concept in the schema, so this looks like a generator artefact rather than a modelled behaviour and no exercise in this section depends on it (owner of the fix: `dataset-engineer`). Section 33 (depuración de consultas) was authored on 2026-09-22: 3 theory lessons (decompose and count; joins that multiply; filters, NULL and grain), 12 theory questions covering all eight question types and 7 verified exercises on `tiendaviva` (advanced -> expert). Counts reported by `npm run content:validate` after that change: 33 of 39 sections published, 281 lessons, 342 theory questions, 167 exercises over 4 datasets. Every exercise shows a plausible-but-wrong query in the scenario and asks for the correct result, and every expert explanation carries both the right and the wrong number so the size of the error is visible (AR delivered revenue 1 705 368 745,77 against the fan-out figure 3 631 177 874,05; AR/app average ticket 431 786,43 against 552 188,44; Andes Express delivery time 131,11 h against 121,38 h; `NOT IN` over the nullable `categories.parent_id` returning 0 rows instead of 24). **EXPLAIN is not usable in the sandbox today** (checked empirically, not assumed): `pgsql-ast-parser` has no grammar for it, so `parse(...)` throws at column 9 and `gateSql` returns `parse_error` before the statement-kind allowlist is ever consulted — widening an exercise's `allowed_statements` cannot help. PGlite itself runs `EXPLAIN`, `EXPLAIN (ANALYZE)` and `EXPLAIN (ANALYZE, FORMAT JSON)` fine, including after `SET ROLE` to a select-only role, returning a single `QUERY PLAN` column. Making sections 34 (fundamentos de optimización) and 35 (índices y planes de ejecución) teachable in the sandbox therefore needs a gate change owned by `sql-sandbox-engineer` (a leading `EXPLAIN [ ( options ) ]` prefix stripped for analysis only, in the spirit of the existing `INTERSECT`/`EXCEPT` and window-frame rewrites, plus an `explain` statement kind). Section 33 was designed to need none of it: debugging by decomposition is fully teachable with plain `SELECT`. No solution relies on named `WINDOW` clauses or interval frame bounds, and the only calendar-sensitive claims are stated in UTC. Section 37 (casos de negocio) was authored on 2026-09-22: 3 theory lessons (turning a vague request into a specification; assumptions that get written down; sanity-checking a result before sending it), 12 theory questions covering all eight question types and 7 verified exercises spread over all four datasets (intermediate -> expert). Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 36 of 39 sections published, 314 lessons, 378 theory questions, 188 exercises over 4 datasets. Because the section is deliberately open-ended, every statement lists the assumptions that are fixed for grading (grain, which statuses count, the period with its time zone, the minimum-volume threshold, the currency rule) so the expected result stays the only correct answer; the assumptions the learner still owns are named as such. The business stories are live in the data and each expert explanation carries the numbers: Mexican delivered revenue falls 2 075 796,94 -> 1 879 865,71 between July and August 2025, and the drill-down by channel shows marketplace_partner losing half its revenue (167 484,06 -> 83 969,93) on the same order count while the app loses volume; payment failure rates by merchant category land between 3,62 % and 6,03 %, so the honest answer to Risk is that their hypothesis does not hold; Ritmo MRR at 2025-08-31 is 818 live subscriptions that cannot be summed across six currencies; Bolsillo USD volume falls 55 % from January to August 2025 entirely through active users (281 -> 126) with a flat ticket; and the BIENVENIDA coupon shows +5 pp repeat purchase on 177 self-selected customers, which the explanation reports as an association, not an effect. Every timestamp is pinned with `AT TIME ZONE 'UTC'` and the statements say "en UTC"; no solution relies on named `WINDOW` clauses or interval frame bounds. Section 39 (proyectos finales, the capstone) was authored on 2026-09-22: 3 theory lessons (planning an analysis before writing SQL; self-review before delivery; what to do after the course and how the certificate works), 12 theory questions covering all eight question types and 7 verified exercises spread over `pidelo`, `tiendaviva`, `bolsillo` and `ritmo` (advanced -> expert), each one a small end-to-end project with an explicit metric definition, a stated period in UTC and an actionable result. Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 37 of 39 sections published, 325 lessons, 390 theory questions, 195 exercises over 4 datasets. No exercise depends on `bolsillo.accounts.balance` (known generator bug), and the TiendaViva money exercises count a payment as captured when its status is `approved` **or** `refunded`, because a returned order's payment row is `refunded`. Sandbox limit found while authoring: the parser gate rejects `FILTER` applied to an ordered-set aggregate (`percentile_cont(...) WITHIN GROUP (...) FILTER (...)`), so the median in the P2P transfers exercise is restricted with a `CASE` inside the `ORDER BY`; `DISTINCT ON`, `FILTER` on plain aggregates, `count(DISTINCT ...) FILTER`, `sum(count(*)) OVER ()` and interval arithmetic in expressions all pass. Sections 9–13 (text, numeric, dates, CASE, ordering) were authored in parallel by `content-author` agents and wired in with `node scripts/register-content.mjs <slug>`; that script is the only supported way to touch `src/content/index.ts`, because several authors would otherwise edit the same registry. Section 38 (desafíos de entrevista) was authored on 2026-09-22: 3 theory lessons (the recurring interview patterns, how to approach an unfamiliar question out loud, the mistakes that cost offers), 12 theory questions covering all eight question types and 7 verified exercises on all four datasets (intermediate -> expert). Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 35 of 39 sections published, 303 lessons, 366 theory questions, 181 exercises over 4 datasets. The section is built so that each classic trap is the point of its exercise and is live in the data: `dense_rank` vs `rank`/`row_number` for "second highest per group" (28 rows; `rank() = 2` silently drops Ciclismo, Construcción and Muebles, whose top place is tied), `NOT IN` over a nullable column (73 idle couriers with `NOT EXISTS`, 0 rows with `NOT IN`, because 101 cancelled August orders have a NULL `courier_id`), a filtered `LEFT JOIN` with `count(col)` (300 merchants, 249 of them zero; the `WHERE` variant returns 51), case-insensitive duplicate emails (37 groups normalized vs 2 unnormalized), gaps-and-islands streaks (81 listeners with a streak >= 10 days, max 26), `lag(col, 2)` for three-in-a-row (35 cases) and a cumulative-share cutoff (54 of 400 restaurants make 50% of GMV; `acumulado <= 50%` would return 53 and miss the threshold). Every timestamp is pinned with `AT TIME ZONE 'UTC'` and the statements say "en UTC"; `DISTINCT ON` is used (it passes the gate) and no solution relies on named `WINDOW` clauses or interval frame bounds. Section 36 (SQL analítico avanzado) was authored on 2026-09-22: 3 theory lessons (layered queries with CTE, windows and LATERAL; subtotals with ROLLUP/CUBE and GROUPING; arrays and JSON), 12 theory questions covering all eight question types and 7 verified exercises spread over all four datasets (advanced -> expert). Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 36 of 39 sections published, 314 lessons, 378 theory questions, 188 exercises over 4 datasets. The section was designed against the parser gate, checked empirically with `gateSql` before writing anything: `ROLLUP`, `CUBE`, `grouping()`, `LATERAL` (all three spellings), `array_agg`/`array_to_string`/`cardinality`/`unnest`/`= ANY`, `string_agg`, `json`/`jsonb` builders and the `->`/`->>` operators, `DISTINCT ON`, `FILTER`, `WITHIN GROUP` and `generate_series` all parse, pass the gate and execute. Four things do **not**: explicit `GROUP BY GROUPING SETS (...)` (parse error at the keyword, in every spelling), `WITH RECURSIVE` without a column list **and** `WITH RECURSIVE` accompanied by any sibling CTE (only the single-CTE, explicit-column-list form parses), array slice syntax `arr[1:3]`, and `WITH ... AS MATERIALIZED`. Named `WINDOW` clauses and interval frame bounds now pass (the gate rewrites them), but no solution in this section depends on them. The gaps are reported to `sql-sandbox-engineer`; the lessons state the `GROUPING SETS` and recursive-CTE limitations explicitly instead of hiding them. Business numbers in the explanations are live: the TiendaViva ROLLUP closes at 13 156 delivered orders and 2615 distinct customers (657+210+559 channel customers in AR against a 777 country subtotal, which is why a distinct count cannot be summed); the Buenos Aires CUBE shows `card` at 1807 orders with a 51 109,33 average ticket against `cash` at 485 and 46 099,13; Bolsillo has no dominant merchant in any category (top share 5,07 %-7,07 %); Pídelo's 14 437 orders follow exactly five event routes, 92,01 % completing the cycle and 150 cancelled after pickup; and the Ritmo jsonb payload top 10 is ~35 % of a 320-artist bimester. Every timestamp filter is a half-open `>= ... < ...` range on explicit `'...Z'::timestamptz` literals and the statements say "en UTC"; money is never summed across currencies (the section counts operations, units or single-currency city totals, and says so in the explanations). Section 34 (fundamentos de optimización) was authored on 2026-09-22: 3 theory lessons (the work the engine does and the logical order of clauses; sargable predicates; reducing rows before joining and aggregating), 12 theory questions covering all eight question types and 7 verified exercises on `pidelo` (very_easy -> expert). Counts reported by `npm run content:validate` after that change, with other sections landing in parallel: 38 of 39 sections published, 336 lessons, 402 theory questions, 202 exercises over 4 datasets. **The section teaches nothing that requires EXPLAIN**, which still fails in `pgsql-ast-parser` before the statement-kind allowlist is consulted (see the section 33 note above); the gate change is still owned by `sql-sandbox-engineer`. Optimisation is taught instead as things the analyst decides before any plan exists: the logical evaluation order, counting rows at each step, sargable date ranges vs `date_trunc`/`extract`/`to_char` on the column, `LIKE 'x%'` vs `LIKE '%x'`, `OR` across columns vs `UNION`, `SELECT *`, `LIMIT` without a unique tie-break, `EXISTS`/`IN` vs `JOIN + DISTINCT`, `NOT EXISTS` vs `NOT IN` over a nullable column, and aggregating two child tables separately instead of fan-out. The first lesson states plainly that `EXPLAIN` exists on a real server and is the subject of section 35, and that it is not available in this practice environment. Two exercises enforce the lesson through `prohibited_patterns` (`date_trunc`/`extract`/`to_char` on the sargable-range exercise, `DISTINCT` on the existence exercise, `NOT IN` on the anti-join exercise), and each statement says so explicitly so the constraint is fair. Business numbers in the explanations are live: 1117 delivered July orders out of 1214 placed; the 20 slowest August deliveries top out at 100 minutes with five orders tied at 88, which is why the `LIMIT` needs `ORDER BY minutos_reales DESC, order_id`; 330 of 358 active restaurants delivered in August; Bogotá August units split 321/99/71 across principal, acompañamiento and bebida; in Lima Q3 the fan-out query reports 16 ratings for restaurant 93 instead of 7 and 10 instead of 4 for restaurant 5; 73 of 503 active couriers made no August delivery (`NOT IN` returns 0 rows because 101 cancelled August orders carry a NULL `courier_id`); and the CDMX Q3 top 10 is led by Doña Ortiz 365 with 19 orders, 835,10 MXN average ticket and 15,8 % late deliveries, with four restaurants tied at 15 orders on the cut line. Money is never summed across cities, because Pídelo prices are in each city's currency. A sandbox behaviour worth recording, found while authoring: at the time, the verify/sandbox session inherited the host's zone (observed as `TimeZone = 'Etc/GMT+3'`), so `TIMESTAMPTZ '2025-07-01 00:00:00+00' + INTERVAL '1 month'` landed on 2025-07-31 00:00 UTC rather than 2025-08-01 — month arithmetic on a `timestamptz` is resolved in the session zone. **D-20 superseded this**: the sandbox and verify sessions are now pinned to `TimeZone = 'UTC'`, so that expression lands on 2025-08-01 here and results no longer vary between machines. The session-zone dependency itself is still real on any server that is not in UTC, so it remains a documented common mistake in the first exercise (rewritten to argue from the session zone rather than from `Etc/GMT+3`), and lessons must not promise the learner can observe the variation in this platform — see `docs/CONTENT_GUIDELINES.md` §7b. Overlap to note for `curriculum-designer`: section 38 has a very similar `NOT IN` courier exercise (`entrevista-repartidores-sin-entregas`, same 73 rows with more columns); section 34 introduces the pattern and section 38 revisits it as an interview trap, which reads as a deliberate callback but was not coordinated.

### Section 35 (índices y planes de ejecución) — completed and published (2026-09-23)

The last undeveloped section of the 39. Three theory lessons
(`src/content/lessons/indices-y-planes-de-ejecucion.ts`): what an index is and the B-tree mental
model, selectivity and cardinality, who decides (the planner and its statistics) · how a plan is
read (node tree, `cost`/`rows`/`width`, estimate vs `actual rows`, `loops`, `BUFFERS`, the four
read nodes, and three real captured plans) · the three join strategies, composite-index column
order and the left-prefix rule, covering indexes and `INCLUDE`, the cost of an index on writes,
and how to bring an index request to a data engineer. Twelve theory questions covering all eight
question types and seven verified exercises on `pidelo` (very_easy → expert).

Counts reported by `npm run content:validate` after the section landed: 39 of 39 sections
published, 347 lessons, 414 theory questions, 209 exercises over 4 datasets. `npm run
content:verify`: OK, 209 exercises.

**Section 39 «SQL con IA: pedir, verificar y corregir» (D-40, 2026-09-24).** Inserted before the
capstone, which became section 40; mandatory, and required by `analista-sql-profesional`. Five
theory lessons (the first free), eight exercises plus a section challenge and a bank of twelve
questions. It teaches using the learner's own AI assistant for SQL and, above all, verifying its
answer: every exercise shows an authored "AI answer" with a real defect — another dialect
(`IFNULL`, `DATE_FORMAT`, `TOP`, `GETDATE()`), an invented column, a `BETWEEN` month border on a
`timestamptz`, a silent `INNER JOIN` that drops zeros, a fan-out join that inflates revenue
(279.3M vs 107.0M), ties lost to `row_number()` — and each defect was proven to change the result on
the snapshot. Nothing calls an AI at runtime; the «Copiar contexto para tu IA» button
(`src/config/ai.ts`) copies schema and task only, in this section only. Counts after it landed:
40 of 40 sections published, 362 lessons, 444 theory questions, 218 exercises; `content:verify`
OK (218), `content:claims` OK (72), `content:figures` OK (201).

**The three plans in the lesson are real captures**, taken with
`EXPLAIN (ANALYZE, BUFFERS)` against the `pidelo` v1 snapshot with statistics refreshed, and
pasted verbatim. They turned out to be unusually good teaching material: on the **same** table, a
July range that passes 1214 of 14 437 rows gets a `Bitmap Index Scan` and finishes in 2,3 ms,
while `status = 'delivered'`, which passes 13 284 of 14 437, gets a `Seq Scan` and takes 24,5 ms
with `Rows Removed by Filter: 1153`. The planner chose correctly both times, which is the whole
"when an index helps and when it does not" lesson in two plans. The third capture shows
`Hash Join`, `Hash`, `HashAggregate` and a `Bitmap Index Scan` together, with the 400-row
`restaurants` on the hash side. Estimate-vs-actual is honest and close in all three (1206/1214,
1286/1280, 400/375), which the prose uses as the baseline against which the pathological case is
recognisable. Two caveats are stated in the prose, as required: the snapshot is small (14 437
orders), and the captures show `shared hit` only — every block was already in memory, so the cost
of a scattered row fetch, which is what makes a bad index choice expensive on a real table, is
invisible here.

Design constraint that shapes the whole section: **a learner cannot run `EXPLAIN`**. The parser
gate (`src/lib/sandbox/gate.ts`) fails with `parse_error` because `pgsql-ast-parser` has no
grammar for it, before the statement-kind allowlist is consulted, so widening
`allowed_statements` cannot help (first recorded while authoring section 33). Section 35 therefore
follows the section 34 precedent: the learner writes the better query or produces the measurement,
and the plan-level reasoning lives in the scenario and the expert explanation. The delta over
section 34 is quantitative and vocabulary-based — 34 teaches "write SQL that does less work", 35
teaches "measure selectivity and cardinality, name the node and join strategy, and propose an
index with evidence". The seven exercises follow that arc: selectivity of each `status` value
(13 284 delivered = 92,01 % against 1153 cancelled = 7,99 %, so the answer to "index `status`?"
is "depends on the value"); cardinality of the candidate columns in one pass (14 437 rows, 4810
customers, 400 restaurants, 600 couriers — the last one ignoring 1153 NULLs, which are exactly the
cancelled orders); the selectivity line that accompanies an index request, measured with
`count(*) FILTER` so the denominator survives (6 rows of 14 437 = 0,0416 %); an equality-plus-range
filter written the way a `(restaurant_id, placed_at)` index can serve, ordering included (19 rows);
a monthly series whose only columns are the two indexed ones, so it is a covering-index candidate
(17 months, with March and August 2024 absent because the restaurant had no orders); the
August close for Ciudad de México, which is the third captured plan (72 restaurants, led by
El Rincón Medina 221 with 8 orders and 8309,34); and an expert selectivity report over five
production filters in a single pass, with a three-branch `CASE` verdict. Money is never summed
across cities, because `pidelo` prices are in each city's currency; the two money exercises are
restricted to one city and say so.

Question bank notes: Q6 interprets one of the three real captures. Q7 needs a pathological
estimate/actual gap (estimated 100, actual 912 430) that the healthy captures do not contain, so
its plan is presented as a **labelled schematic with placeholder costs** and the prompt says so
explicitly — never as the real plan of a real query.

## 4. MVP datasets (see `src/datasets/<slug>/README.md` once generated)

| Slug         | Domain                                          | Story                                             | Tables                                                                                               | Highlights                                                                                                                                                                                                              |
| ------------ | ----------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tiendaviva` | E-commerce marketplace (AR, MX, CO, CL, PE, UY) | Marketplace of independent sellers                | customers, sellers, products, categories, orders, order_items, payments, shipments, reviews, returns | multi-currency (ARS, MXN, COP, CLP, PEN, UYU), cuotas, cancellations, seasonality (Hot Sale, Buen Fin, Navidad), duplicate customers, NULL shipping dates                                                               |
| `bolsillo`   | Digital wallet / fintech                        | Wallet with transfers, top-ups, card payments, QR | users, accounts, transactions, transfers, merchants, cards, kyc_events, fx_rates                     | status transitions, reversals, timezone-aware timestamps, fraud flags, monthly cohorts                                                                                                                                  |
| `pidelo`     | Food delivery                                   | Restaurants, couriers, orders in several cities   | cities, restaurants, menu_items, customers, couriers, orders, order_events, ratings, promotions      | funnel events, delivery SLA, weather-free seasonality, promo abuse, missing ratings                                                                                                                                     |
| `ritmo`      | Music streaming (MX, BR, AR, CO, CL, PE)        | Latin music streaming service, freemium           | users, artists, albums, tracks, plays, playlists, playlist_tracks, subscriptions, follows            | cohorts and retention, signup→play→playlist→paid funnel, running totals and moving averages, artist rankings per country/month, LAG/LEAD on play history, planted duplicate plays, dirty seconds_played and NULL device |

Later datasets (post-MVP): ride-hailing (`rutaya`), SaaS subscriptions (`nubeflow`), education (`aulaviva`), social content (`redlatina`), event ticketing, logistics, customer support.

## 5. Section template (every section must fill all fields)

1. Objetivos de aprendizaje (3–5, verbos observables)
2. Teoría concisa (≤ 900 palabras por lección) con ejemplos ejecutables
3. Ejemplo resuelto paso a paso (worked example)
4. Errores comunes (≥ 3)
5. Ejercicios prácticos (progresivos; al menos uno por nivel de dificultad presente)
6. Preguntas de teoría (≥ 8, tipos variados; el quiz sirve una muestra — largo por sección en §3)
7. Desafío de sección (intermedio/avanzado, integra los conceptos)
8. Requisitos de completado: todos los ejercicios correctos + quiz ≥ 80 % + desafío
9. Elegibilidad de certificado (si aplica)

## 6. Exercise model (authored as JSON validated by `src/content/schemas/exercise.ts`)

`slug, title, section, difficulty, estimated_minutes, concepts[], prerequisites[], dataset, scenario_md, business_question_md, learning_objective, theory_ref, tables_used[], expected_columns[{name,type}], validation_rules{order_matters, numeric_tolerance, allow_extra_columns:false, dedupe:false, required_concepts[], prohibited_patterns[], max_execution_ms}, reference_solution, alternative_solutions[], hints[3], common_mistakes[], expert_explanation_md, improvement_feedback[{condition, message}], reward{xp, coins, hint_penalty, solution_penalty}, solution_unlock{min_attempts:3, min_hints:2, min_minutes:10, allow_explicit:true}`

## 7. Pedagogical rules applied

Scaffolding (hints), retrieval practice (quizzes + review sessions), spaced repetition (review queue based on failed questions and hint-heavy exercises), worked examples before first exercise of a concept, immediate structured feedback, interleaving (section challenges mix prior concepts), mastery gates per level, reflection prompt after each section, authentic business scenarios.
