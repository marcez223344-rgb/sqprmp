import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Currículo" };

export default function Page() {
  return <PlaceholderPage title="Currículo" phase={3} />;
}
