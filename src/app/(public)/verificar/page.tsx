import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Verificar certificado" };

export default function Page() {
  return <PlaceholderPage title="Verificar certificado" phase={7} />;
}
