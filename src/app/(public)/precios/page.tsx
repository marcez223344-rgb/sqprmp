import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Precios" };

export default function Page() {
  return <PlaceholderPage title="Precios" phase={6} />;
}
