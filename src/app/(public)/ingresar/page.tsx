import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Ingresar" };

export default function Page() {
  return <PlaceholderPage title="Ingresar" phase={2} />;
}
